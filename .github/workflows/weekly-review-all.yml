name: "Weekly AI Review — Active Repos Only"

on:
  schedule:
    - cron: '0 6 * * 0'
  workflow_dispatch:
    inputs:
      single_repo:
        description: "Review only this repo (leave blank for active repos)"
        required: false
        default: ""
      activity_days:
        description: "Consider repos active if pushed within N days (default: from config)"
        required: false
        default: ""
      provider:
        description: "AI provider override (leave blank to use config.yml)"
        required: false
        type: choice
        options:
          - ""
          - "gemini"
          - "claude"

jobs:
  discover:
    name: "Discover active repositories"
    runs-on: ubuntu-latest
    outputs:
      repos: ${{ steps.list.outputs.repos }}
      provider: ${{ steps.config.outputs.provider }}
      model: ${{ steps.config.outputs.model }}
    steps:
      - name: Checkout hub
        uses: actions/checkout@v4

      - name: Read config
        id: config
        run: |
          pip install -q pyyaml

          python3 -c "
          import yaml, os
          with open('config.yml') as f:
              cfg = yaml.safe_load(f)

          # Provider: input override > config
          override = '${{ github.event.inputs.provider }}'.strip()
          provider = override if override else cfg.get('provider', 'gemini')
          model = cfg.get(provider, {}).get('weekly_model', '')

          with open(os.environ['GITHUB_OUTPUT'], 'a') as out:
              out.write(f'provider={provider}\n')
              out.write(f'model={model}\n')
          print(f'✓ Provider: {provider}, Model: {model}')
          "

      - name: Find active repos
        id: list
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          OWNER=$(grep 'github_owner' config.yml | awk '{print $2}' | tr -d '"')
          
          # Activity days: input > config > default 7
          INPUT_DAYS="${{ github.event.inputs.activity_days }}"
          CONFIG_DAYS=$(python3 -c "
          import yaml
          with open('config.yml') as f:
              print(yaml.safe_load(f).get('activity_days', 7))
          ")
          ACTIVITY_DAYS="${INPUT_DAYS:-$CONFIG_DAYS}"

          EXCLUDE=$(python3 -c "
          import yaml, json
          with open('config.yml') as f:
              cfg = yaml.safe_load(f)
          print(json.dumps(cfg.get('exclude_repos', [])))
          ")

          INCLUDE_ONLY=$(python3 -c "
          import yaml, json
          with open('config.yml') as f:
              cfg = yaml.safe_load(f)
          print(json.dumps(cfg.get('include_only', [])))
          ")

          REPOS_RAW=$(gh api --paginate "/users/${OWNER}/repos?per_page=100&type=owner" \
            --jq '[.[] | select(.fork == false and .archived == false) | {name: .name, pushed_at: .pushed_at}]')

          FILTERED=$(python3 -c "
          import json, sys
          from datetime import datetime, timedelta, timezone

          repos_raw = json.loads('''$REPOS_RAW''')
          exclude = json.loads('$EXCLUDE')
          include_only = json.loads('$INCLUDE_ONLY')
          single = '${{ github.event.inputs.single_repo }}'.strip()
          activity_days = int('$ACTIVITY_DAYS')

          cutoff = datetime.now(timezone.utc) - timedelta(days=activity_days)
          active = []
          skipped = []

          for r in repos_raw:
              name = r['name']
              pushed = datetime.fromisoformat(r['pushed_at'].replace('Z', '+00:00'))

              if single:
                  if name != single:
                      continue
              elif include_only:
                  if name not in include_only:
                      continue
              else:
                  if name in exclude:
                      continue

              if pushed >= cutoff:
                  active.append(name)
              else:
                  skipped.append(f'{name} (last push: {r[\"pushed_at\"][:10]})')

          print(json.dumps(active))
          for s in skipped:
              print(f'💤 Skipped inactive: {s}', file=sys.stderr)
          ")

          echo "repos=$FILTERED" >> $GITHUB_OUTPUT

          ACTIVE_COUNT=$(echo "$FILTERED" | python3 -c 'import json,sys; print(len(json.load(sys.stdin)))')
          echo "## 📋 Repository Discovery" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Provider:** ${{ steps.config.outputs.provider }}" >> $GITHUB_STEP_SUMMARY
          echo "**Activity window:** last ${ACTIVITY_DAYS} days" >> $GITHUB_STEP_SUMMARY
          echo "**Active repos:** ${ACTIVE_COUNT}" >> $GITHUB_STEP_SUMMARY
          echo "**Will review:** ${FILTERED}" >> $GITHUB_STEP_SUMMARY

  review:
    name: "Review ${{ matrix.repo }}"
    needs: discover
    if: needs.discover.outputs.repos != '[]'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        repo: ${{ fromJson(needs.discover.outputs.repos) }}
    permissions:
      contents: read

    steps:
      - name: Checkout hub (for scripts)
        uses: actions/checkout@v4
        with:
          path: hub

      - name: Clone target repo
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          OWNER=$(grep 'github_owner' hub/config.yml | awk '{print $2}' | tr -d '"')
          git clone --depth 1 "https://x-access-token:${GH_TOKEN}@github.com/${OWNER}/${{ matrix.repo }}.git" target

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          PROVIDER="${{ needs.discover.outputs.provider }}"
          pip install -q pyyaml
          if [ "$PROVIDER" = "claude" ]; then
            pip install -q anthropic
          else
            pip install -q google-genai
          fi

      - name: Run AI review
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          PROVIDER="${{ needs.discover.outputs.provider }}"
          MODEL="${{ needs.discover.outputs.model }}"

          python hub/scripts/ai-review.py \
            --mode full \
            --provider "$PROVIDER" \
            --project-dir target \
            --model "$MODEL" \
            --output review_report.md

          echo "✅ Review completed for ${{ matrix.repo }} (${PROVIDER})" >> $GITHUB_STEP_SUMMARY

      - name: Create issue in target repo
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          OWNER=$(grep 'github_owner' hub/config.yml | awk '{print $2}' | tr -d '"')
          PROVIDER="${{ needs.discover.outputs.provider }}"
          DATE=$(date +%Y-%m-%d)
          REPORT=$(cat review_report.md)

          gh issue list \
            --repo "${OWNER}/${{ matrix.repo }}" \
            --label "ai-code-review,automated" \
            --state open \
            --json number \
            --jq '.[].number' | while read num; do
              gh issue close "$num" --repo "${OWNER}/${{ matrix.repo }}" 2>/dev/null || true
            done

          BODY=$(cat <<ISSUE_EOF
          ${REPORT}

          ---
          *🤖 Автоматически сгенерировано [code-review-hub](https://github.com/${OWNER}/code-review-hub) · ${PROVIDER} · ${DATE}*
          ISSUE_EOF
          )

          gh issue create \
            --repo "${OWNER}/${{ matrix.repo }}" \
            --title "[AI Review] Еженедельный отчёт — ${DATE}" \
            --body "$BODY" \
            --label "ai-code-review,automated" 2>/dev/null || \
          gh issue create \
            --repo "${OWNER}/${{ matrix.repo }}" \
            --title "[AI Review] Еженедельный отчёт — ${DATE}" \
            --body "$BODY"

      - name: Upload report artifact
        uses: actions/upload-artifact@v4
        with:
          name: review-${{ matrix.repo }}
          path: review_report.md
          retention-days: 30

  notify:
    name: "Send notifications"
    needs: [discover, review]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Checkout hub
        uses: actions/checkout@v4

      - name: Download all reports
        uses: actions/download-artifact@v4
        with:
          path: reports

      - name: Build summary & send webhook
        env:
          WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          OWNER=$(grep 'github_owner' config.yml | awk '{print $2}' | tr -d '"')
          DATE=$(date +%Y-%m-%d)
          REPOS='${{ needs.discover.outputs.repos }}'
          PROVIDER='${{ needs.discover.outputs.provider }}'

          SUMMARY="# 📊 Weekly AI Review Summary — ${DATE}\n\n"
          SUMMARY+="**Provider:** ${PROVIDER}\n"
          SUMMARY+="**Active repos reviewed:** $(echo "$REPOS" | python3 -c 'import json,sys; print(len(json.load(sys.stdin)))')\n\n"

          for dir in reports/review-*/; do
            REPO_NAME=$(basename "$dir" | sed 's/review-//')
            if [ -f "$dir/review_report.md" ]; then
              SCORE=$(grep -oP 'Health Score:\s*\K[0-9.]+' "$dir/review_report.md" 2>/dev/null || echo "N/A")
              SUMMARY+="- **${REPO_NAME}**: Health ${SCORE}/10\n"
            fi
          done

          echo -e "$SUMMARY" >> $GITHUB_STEP_SUMMARY

          if [ -n "$WEBHOOK_URL" ]; then
            PAYLOAD=$(jq -n \
              --arg event "weekly_review_summary" \
              --arg date "$DATE" \
              --arg owner "$OWNER" \
              --arg provider "$PROVIDER" \
              --argjson repos "$REPOS" \
              --arg summary "$(echo -e "$SUMMARY")" \
              '{event:$event, date:$date, owner:$owner, provider:$provider, repos:$repos, summary:$summary}')

            curl -sf -X POST "$WEBHOOK_URL" \
              -H "Content-Type: application/json" \
              -d "$PAYLOAD" \
              --max-time 30 || echo "⚠️ Webhook delivery failed"
          fi
